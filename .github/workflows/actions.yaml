name: mtf2json build and tests
on: [push, pull_request]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install poetry flake8
        poetry install --sync
    - name: Lint with ruff
      run: |
        poetry run ruff check
    - name: Check formatting with ruff
      run: |
        poetry run ruff format --check
    - name: Check code with mypy
      run: |
        poetry run mypy . --show-error-context
    - name: Test with pytest
      run: |
        poetry run pytest -v
    - name: Test coverage with pytest-cov
      run: |
        poetry run pytest --cov=mtf2json tests/
    - name: Check out btdata repository
      uses: actions/checkout@v4
      with:
        repository: juk0de/btdata
        path: btdata
    - name: Convert all MTF files
      run: |
        poetry run mtf2json -M btdata/mtf/mechs/biped/ -J btdata/json/mechs/biped/

